// Optional runtime config.
// For GitHub Pages (static mode), leave empty.
// For local/remote backend mode, set window.__API_BASE__ to your backend origin, e.g. "http://127.0.0.1:8010"
window.__API_BASE__ = window.__API_BASE__ || "";
