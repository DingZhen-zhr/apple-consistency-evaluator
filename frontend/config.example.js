// Copy this file to `frontend/config.js` for local testing, or let GitHub Actions generate `config.js` during Pages deploy.
//
// IMPORTANT: no trailing slash.
// Example (Render):
//   window.__API_BASE__ = "https://your-service.onrender.com";
//
// For local dev with FastAPI serving `/ui` + `/api` on the same origin, you can leave it empty:
window.__API_BASE__ = "";
